---
# isaac-tdgt
title: 'Foreman: state-machine orchestration (design draft)'
status: todo
type: task
created_at: 2026-08-22T17:13:32Z
updated_at: 2026-08-22T17:13:32Z
---

Model agent orchestration as explicit state machines (idea via Micah's dad; see slagyr/statemachine — transition tables, Mealy actions, superstates). Today's orchestration is an implicit FSM smeared across skills, band config, cron, and humans; its failures this week were lost-event bugs (orphaned claim = state with no action fired; silent turn death = state no transition exits; continuation-budget hold = manual timeout). DESIGN: (1) Per-orchestration TRANSITION TABLE, actions are prompts (Mealy); superstates for shared transitions (e.g. any-state budget-timeout -> held/notify-human). New project = one table + crew/workspace bindings, replacing the file/crew/session frenzy. (2) Two machine archetypes: per-entity instances (one per bean) for singular steps; SINGLETON ACCUMULATOR machines for collected steps (hardening): collect action appends to basket, guarded drain transition (count >= N / quiescence via cron tick / explicit), action = ONE prompt carrying the basket. (3) STATE-STORE PORT: instances = entity-id -> state + context. Beans are the default adapter (git-backed, auditable, human-EDITABLE — a human flipping status is a manual transition; re-hail is event injection); file/EDN adapter for orchestrations without repos; in-memory for ephemeral fan-outs. Usable entirely without beans. (4) EVENTS, two kinds, no new messaging scheme: SEMANTIC DECLARATIONS via a crew-side `signal` tool (signal :landed {:bean .. :commit ..}) writing event records the foreman consumes — replaces remembered hail choreography with one structured tool call, routing owned by config; INFRASTRUCTURE OBSERVATIONS emitted by the system (turn ended/died via isaac-e04k, workspace freed, commit observed) serving as BACKSTOP transitions: 'turn ended in :claimed with no signal' is itself an event the table handles (nudge/re-dispatch/escalate). The model indicates when it finishes; the machine notices when it doesn't. (5) ACTIONS ride hail unchanged (durable, deferred, attention). Composition: foreman decides WHAT, dispatch-gate/workspace module decides WHERE, hail carries, cron ticks, hooks route. New module — NOT hail (hail stays pure messaging; three-time ruling this week). Name note: 'isaac-foreman' was hallucinated into scrapper's gists by grok-4.20 two days before this design existed — confabulation as prophecy. STATUS: ponder pile with the workspace module (isaac-51xy decisions 36-39); no build until Micah rules.


## Addendum (2026-08-22, Micah): external event triggering

Events must be triggerable externally -> the foreman registers a route through the EXISTING isaac-hooks module (its charter is inbound webhook routing; already live on zanebot for Zaap telemetry). `POST /hooks/foreman` behind the server's existing token auth. **Two doors, one intake**: the crew-side `signal` tool and the external endpoint write the same event envelope to the same durable queue. Durability rule (hails-never-die for events): persist-before-ack — append to disk, then 200; the machine consumes from disk, so an accepted event fires even across restarts. Unlocked table inputs: GitHub Actions posting :ci-passed (CI as first-class transition, not a report a crew reads), human curl for manual events, remote cron ticks, cross-host isaac signaling.
